language: node_js
branches:
  only:
    - master
install:
  - npm install -g markdown-pdf
script:
  - markdown-pdf *.md
deploy:
  provider: releases
  file: "*.pdf"
  skip_cleanup: true
  on:
    tags: true